package bracketchecker

/*import "fmt"

//Bracketchecker checks for brackets
func Bracketcheker(s string) bool {
	var openedbrackets string = "{(["
	var closedbrackets string = "})]"
	lastbrackettype := openedbrackets[0]
	var brackettypecounter0 int =0
	var brackettypecounter1 int = 0
	var brackettypecounter2 int =0
	lastbrackettypeplusone := closedbrackets[0]
	for i := 0; i < len(s)-1; i++ {
		if s[i] == openedbrackets[0] || s[i] == openedbrackets[1] || s[i] == openedbrackets[2] {
			lastbrackettype = s[i]
		}
		if s[i+1] == openedbrackets[0] || s[i+1] == openedbrackets[1] || openedbrackets[2] == s[2] {
			lastbrackettypeplusone = s[i+1]
		} else if s[i+1] == closedbrackets[0] || s[i+1] == closedbrackets[1] || s[i+1] == closedbrackets[2] {
			fmt.Println("wrong brackets")
		}
		if s {

		}
	}
}*/
